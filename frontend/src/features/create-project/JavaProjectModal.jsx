import React from "react";
import JavaProjectForm from "./JavaProjectForm";

const JavaProjectModal = ({ onClose }) => {
  return <JavaProjectForm onClose={onClose} />;
};

export default JavaProjectModal;
